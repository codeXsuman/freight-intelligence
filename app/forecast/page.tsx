'use client'; import {useEffect,useState} from 'react'; import Nav from '@/components/Nav'; import {ResponsiveContainer,LineChart,Line,XAxis,YAxis,Tooltip,CartesianGrid,Legend} from 'recharts';
export default function Forecast(){const [data,setData]=useState<any[]>([]);useEffect(()=>{fetch('/api/forecast').then(r=>r.json()).then(x=>{const h=x.history??[],f=x.forecast??[];setData([...h.map((d:any)=>({period:d.period,actual:d.value})),...f.map((d:any)=>({period:d.period,forecast:d.value,low:d.low,high:d.high}))])})},[]);return <><Nav/><main className="container"><div className="eyebrow">Forecasting engine</div><h1>Freight rate outlook</h1><p className="muted">Indonesia → Paradip · Panamax. Forecast values are model estimates based on seeded historical data.</p><div className="card chart" style={{marginTop:24}}><ResponsiveContainer width="100%" height="100%"><LineChart data={data}><CartesianGrid stroke="#213149" strokeDasharray="3 3"/><XAxis dataKey="period" stroke="#7f91a7"/><YAxis stroke="#7f91a7"/><Tooltip contentStyle={{background:'#0d1a2b',border:'1px solid #263a53'}}/><Legend/><Line dataKey="actual" name="Historical $/t" stroke="#55d6be" strokeWidth={3} connectNulls/><Line dataKey="forecast" name="Forecast $/t" stroke="#f6c85f" strokeWidth={3} strokeDasharray="6 4" connectNulls/><Line dataKey="low" name="Lower bound" stroke="#71839a" strokeDasharray="3 3" connectNulls/><Line dataKey="high" name="Upper bound" stroke="#71839a" strokeDasharray="3 3" connectNulls/></LineChart></ResponsiveContainer></div><div className="notice"><b>Next step for SIH:</b> replace this transparent baseline forecaster with an evaluated XGBoost/ARIMA model trained on validated freight and market data, while preserving this API contract.</div></main></>}
