'use client'; import Link from 'next/link'; import {useRouter} from 'next/navigation'; export default function Nav({name}:{name?:string}){const r=useRouter();async function logout(){await fetch('/api/auth/logout',{method:'POST'});r.push('/login')}return <nav className="nav"><Link href="/dashboard" className="brand">Freight<span>IQ</span></Link><div className="navlinks"><Link href="/dashboard">Dashboard</Link><Link href="/forecast">Forecast</Link><Link href="/optimize">Optimize</Link><Link href="/scenarios">Scenarios</Link></div><div className="row"><span className="muted">{name||'Planner'}</span><button className="btn secondary" onClick={logout}>Logout</button></div></nav>}
